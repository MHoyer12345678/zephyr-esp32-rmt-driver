if(CONFIG_DT_HAS_ESPRESSIF_ESP32_RMT_ENABLED)
  zephyr_library()
  zephyr_library_sources(drvr/src/esp32-rmt.c)
  zephyr_include_directories(drvr/inc)
  if (CONFIG_NEC4_IR_DECODER)
    zephyr_library_sources(lib/src/nec-ir-decoder.c)
    zephyr_include_directories(lib/inc)
  endif()
endif()
